---
"on":
  push:
    paths-ignore:
      - "**.md"
      - doc/*
  pull_request:
    paths-ignore:
      - "**.md"
      - doc/*
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: leafo/gh-actions-lua@v12
        with:
          luaVersion: "5.1"
      - uses: leafo/gh-actions-luarocks@v6
      - uses: xmake-io/github-action-setup-xmake@v1
      - name: Install dependencies
        run: |
          luarocks install luanativeobjects
          sudo apt-get -y update
          sudo apt-get -y install librime-dev
      - uses: lumen-oss/luarocks-tag-release@v7
        env:
          LUAROCKS_API_KEY: ${{secrets.LUAROCKS_API_KEY}}
        if: "! startsWith(github.ref, 'refs/tags/')"
        with:
          extra_luarocks_args: |
            --force
          version: scm
          template: rime.nvim-scm-1.rockspec
      - uses: lumen-oss/luarocks-tag-release@v7
        env:
          LUAROCKS_API_KEY: ${{secrets.LUAROCKS_API_KEY}}
        if: startsWith(github.ref, 'refs/tags/')
        with:
          extra_luarocks_args: |
            --force
          template: rime.nvim-scm-1.rockspec
      - if: startsWith(github.ref, 'refs/tags/')
        env:
          LUAROCKS_API_KEY: ${{secrets.LUAROCKS_API_KEY}}
        run: |
          luarocks install rime.nvim
          luarocks pack rime.nvim
          scripts/update.sh ./*.rock

  deploy:
    needs: build
    runs-on: macos-latest
    steps:
      - uses: leafo/gh-actions-lua@v12
        with:
          luaVersion: "5.1"
      - uses: leafo/gh-actions-luarocks@v6
      - uses: xmake-io/github-action-setup-xmake@v1
      - name: Install dependencies
        run: |
          luarocks install luanativeobjects
          sudo apt-get -y update
          sudo apt-get -y install librime-dev
      - if: startsWith(github.ref, 'refs/tags/')
        env:
          LUAROCKS_API_KEY: ${{secrets.LUAROCKS_API_KEY}}
        run: |
          luarocks install rime.nvim
          luarocks pack rime.nvim
          scripts/update.sh ./*.rock
